[pytest]
addopts = --html=selenium/reports/selenium_report.html --self-contained-html
markers =
    e2e: browser-based end-to-end test
    smoke: critical-path verification
    regression: regression coverage
    sanity: quick feature verification
    functional: functional behavior verification
    ui: visual and navigation verification
